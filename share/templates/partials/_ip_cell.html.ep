% # Contents of one results-table IP cell: a clickable filter link for the IP, a
% # "?" button opening the layout's IP info modal, and the GeoIP country flag
% # with subdivision and (toggleable) city. Expects $ip; renders nothing when
% # the IP is unset. Shared by the suricata/sagan/baphomet search result tables.
% if ($ip) {
<a href="#" class="ip-filter" data-ip="<%= $ip %>"><%= $ip %></a> <%= include 'partials/_info_btn', kind => 'ip', value => $ip %><% if (my $country = $self->ip_country($ip)) { %> <span class="text-muted small" title="<%= $country %>"><%= $self->country_flag($country) %> <%= $country %><% if (my $subdivision = $self->ip_subdivision($ip)) { %>/<%= $subdivision %><% } %><% if (my $city = $self->ip_city($ip)) { %><span class="geoip-city"> <%= $city %></span><% } %></span><% } %>
% }
